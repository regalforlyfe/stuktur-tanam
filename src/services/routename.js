const home = "/";
const dashboard = "/dashboard";
const product = "/product";
const buku = "/buku";
const login = "/login";
const register = "/register";

const RouteName = {
  home,
  dashboard,
  buku,
  product,
  login,
  register,
};

export default RouteName;
